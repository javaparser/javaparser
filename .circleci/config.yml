# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/reference/configuration-reference
version: 2.1

orbs:
  gradle: circleci/gradle@3.0.0

# Define reusable executors for different OS and JDK combinations
executors:
  ubuntu-jdk21:
    docker:
      - image: cimg/openjdk:21.0
    resource_class: large
  ubuntu-jdk25:
    docker:
      - image: cimg/openjdk:25.0
    resource_class: large

# Define a job for running tests
# See: https://circleci.com/docs/guides/orchestrate/jobs-steps/#jobs-overview & https://circleci.com/docs/reference/configuration-reference/#jobs
jobs:
  gradle_test:
    resource_class: large
    parameters:
      executor_name:
        type: string
      jdk_version:
        type: string
    executor: << parameters.executor_name >>
    environment:
      JDK: << parameters.jdk_version >>
    steps:
      # Checkout the code as the first step.
      - checkout

      # Cache Gradle dependencies between builds
      - restore_cache:
          keys:
            - gradle-cache-{{ checksum "build.gradle.kts" }}-{{ checksum "gradle/wrapper/gradle-wrapper.properties" }}
            - gradle-cache-

      - run:
          name: Download dependencies
          command: ./gradlew dependencies --no-daemon

      - save_cache:
          paths:
            - ~/.gradle/wrapper
            - ~/.gradle/caches
          key: gradle-cache-{{ checksum "build.gradle.kts" }}-{{ checksum "gradle/wrapper/gradle-wrapper.properties" }}


      # Echo exact java version being used
      - run:
          name: "Echo exact java version"
          command: java -version

      # Actually perform the tests with Gradle
      - run:
          name: "Test with Gradle"
          command: |
            chmod +x ./gradlew
            ./gradlew --continue --parallel --no-daemon test

      - run:
          name: Save test results
          command: |
            mkdir -p ~/test-results/junit/
            find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} ~/test-results/junit/ \;
          when: always
      - store_test_results:
          path: ~/test-results

      # Store test artifacts for reporting
      - store_artifacts:
          path: "**/build/reports/tests/*"
          destination: test-report

# Orchestrate jobs using workflows
# See: https://circleci.com/docs/guides/orchestrate/workflows/ & https://circleci.com/docs/reference/configuration-reference/#workflows
workflows:
  gradle_tests_workflow:
    jobs:
      # Ubuntu runners
      - gradle_test:
          name: "test-ubuntu-jdk21"
          executor_name: ubuntu-jdk21
          jdk_version: "21"
